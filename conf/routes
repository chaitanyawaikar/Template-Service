# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page

GET     /template-service/api/setup                                               controllers.TemplateController.setup
GET     /template-service/api/templates/ids/:id                                   controllers.TemplateController.getTemplateById(id: Int)
GET     /template-service/api/templates/keys/:key                                 controllers.TemplateController.getTemplateByKey(key : String)

# Map static resources from the /public folder to the /assets URL path
#GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
